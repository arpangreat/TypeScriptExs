function sum(num1: number, num2: number, showResult: boolean, phrase: string) {
    if (showResult) {
       console.log("I am created by Swastik Acharyya") 
    } else {
        console.log("I am created by GitHub");
    }
    console.log(phrase);
    return num1 + num2;
}
 
const number1 = '5';
const number2 = 8.9;
const printResult = true;
const resultPhrase = "Yay.....We have done it";

const result = sum(+number1, +number2, printResult, resultPhrase);

console.log(result);
